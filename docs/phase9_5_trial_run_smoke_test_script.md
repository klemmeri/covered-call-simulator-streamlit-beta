# Phase 9-5 — Trial-run smoke-test script

This checkpoint creates the beta trial-run smoke-test script.

It is add-only and makes no dashboard or engine change.

The script covers:

- opening the hosted or local beta app;
- running the default synthetic scenario;
- confirming historical mode is explicit opt-in only;
- reviewing historical-data caution wording;
- running a basic covered-call scenario;
- reviewing charts, tables, risk wording, and feedback capture.

Run:

`app\run_paid_simulator_phase9_5_trial_run_smoke_test_script_check.py`
