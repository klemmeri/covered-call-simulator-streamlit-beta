# Phase 3I-4 Risk-Warning Repair 2

This repair restores the expected function name:

- `render_customer_risk_warning_verification`

It also includes the exact risk term `capped` and an installer that appends the exact dashboard marker expected by the Phase 3I-4 checkpoint:

- `PHASE3H_7_PUBLIC_CUSTOMER_ACTIVATION_ENABLED`

The dashboard marker block is inert and does not change route logic.
